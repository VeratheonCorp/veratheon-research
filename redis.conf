# RDB snapshotting rules
save 10 1

# You can add more rules if needed, e.g., for bursts:
# save 1 100  # Save if 100 changes in 1 second

# Directory for persistence files (matches your Docker volume)
dir /data

# Optional: Disable AOF if you don't want it (default is no anyway)
appendonly no